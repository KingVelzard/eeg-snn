"""Lightweight measurement utilities for the LIF SNN classifier.

Modules:
    model_stats  - parameter and storage counts.
    firing_rate  - mean spike rate per LIF layer over a dataloader.
    latency      - single-trial and batched inference timing.
    report       - CLI that prints all of the above as one report.
"""
